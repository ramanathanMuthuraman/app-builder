path     = require 'path'
rootPath = path.normalize __dirname + '/..'
env      = process.env.NODE_ENV || 'development'

config =
  development:
    root: rootPath
    app:
      name: 'temp'
    port: 3000
    db: 'mysql://localhost/temp-development'
    

  test:
    root: rootPath
    app:
      name: 'temp'
    port: 3000
    db: 'mysql://localhost/temp-test'
    

  production:
    root: rootPath
    app:
      name: 'temp'
    port: 3000
    db: 'mysql://localhost/temp-production'
    

module.exports = config[env]
